/*using System;
public class Prompt
{
    // Declare a variable to store the user input
            string input = "";

            // Prompt the user to press enter or type quit
            Console.WriteLine("Press enter to continue or type quit to exit.");

            // Use a while loop to repeat until the user types quit
            while (input != "quit")
            {
                // Read the user input from the console
                input = Console.ReadLine();
                // If the user pressed enter, print a message
                if (input == "")
                {
                    Console.WriteLine("You pressed enter.");
                }
                // If the user typed quit, print a farewell message
                else if (input == "quit")
                {
                    Console.WriteLine("Goodbye!");
                }
                // If the user typed anything else, print an error message
                else
                {
                    Console.WriteLine("Invalid input. Please press enter or type quit.");
                }
            }
        }*/
    
            
